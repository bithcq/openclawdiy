diff --git a/src/gateway/server-channels.test.ts b/src/gateway/server-channels.test.ts
index 01dd6aa17..8d71de596 100644
--- a/src/gateway/server-channels.test.ts
+++ b/src/gateway/server-channels.test.ts
@@ -5,6 +5,7 @@ import {
   type SubsystemLogger,
   runtimeForLogger,
 } from "../logging/subsystem.js";
+import { registerPluginHttpRoute } from "../plugins/http-registry.js";
 import { createEmptyPluginRegistry, type PluginRegistry } from "../plugins/registry.js";
 import { getActivePluginRegistry, setActivePluginRegistry } from "../plugins/runtime.js";
 import type { PluginRuntime } from "../plugins/runtime/types.js";
@@ -103,6 +104,7 @@ function createManager(options?: {
   channelRuntime?: PluginRuntime["channel"];
   resolveChannelRuntime?: () => PluginRuntime["channel"];
   loadConfig?: () => Record<string, unknown>;
+  pluginRegistry?: PluginRegistry;
 }) {
   const log = createSubsystemLogger("gateway/server-channels-test");
   const channelLogs = { discord: log } as Record<ChannelId, SubsystemLogger>;
@@ -112,6 +114,7 @@ function createManager(options?: {
     loadConfig: () => options?.loadConfig?.() ?? {},
     channelLogs,
     channelRuntimeEnvs,
+    ...(options?.pluginRegistry ? { pluginRegistry: options.pluginRegistry } : {}),
     ...(options?.channelRuntime ? { channelRuntime: options.channelRuntime } : {}),
     ...(options?.resolveChannelRuntime
       ? { resolveChannelRuntime: options.resolveChannelRuntime }
@@ -409,4 +412,35 @@ describe("server-channels auto restart", () => {
 
     expect(manager.isHealthMonitorEnabled("discord", "")).toBe(true);
   });
+
+  it("re-aligns active plugin registry before dynamic channel route registration", async () => {
+    const canonicalRegistry = createEmptyPluginRegistry();
+    const driftedRegistry = createEmptyPluginRegistry();
+    const startAccount = vi.fn(async () => {
+      registerPluginHttpRoute({
+        path: "/api/test-wecom-callback",
+        auth: "plugin",
+        handler: vi.fn(),
+      });
+    });
+
+    canonicalRegistry.channels.push({
+      pluginId: "discord",
+      source: "test",
+      plugin: createTestPlugin({ startAccount }),
+    });
+    setActivePluginRegistry(canonicalRegistry);
+
+    const manager = createManager({ pluginRegistry: canonicalRegistry });
+
+    // 模拟启动过程中其他路径重新加载插件，导致 active registry 漂移。
+    setActivePluginRegistry(driftedRegistry);
+    await manager.startChannels();
+
+    expect(startAccount).toHaveBeenCalledTimes(1);
+    expect(canonicalRegistry.httpRoutes.map((route) => route.path)).toContain(
+      "/api/test-wecom-callback",
+    );
+    expect(driftedRegistry.httpRoutes).toHaveLength(0);
+  });
 });
diff --git a/src/gateway/server-channels.ts b/src/gateway/server-channels.ts
index 16cad24b0..af13c5285 100644
--- a/src/gateway/server-channels.ts
+++ b/src/gateway/server-channels.ts
@@ -6,6 +6,8 @@ import { type BackoffPolicy, computeBackoff, sleepWithAbort } from "../infra/bac
 import { formatErrorMessage } from "../infra/errors.js";
 import { resetDirectoryCache } from "../infra/outbound/target-resolver.js";
 import type { createSubsystemLogger } from "../logging/subsystem.js";
+import type { PluginRegistry } from "../plugins/registry.js";
+import { getActivePluginRegistry, setActivePluginRegistry } from "../plugins/runtime.js";
 import type { PluginRuntime } from "../plugins/runtime/types.js";
 import { resolveAccountEntry } from "../routing/account-lookup.js";
 import {
@@ -77,6 +79,15 @@ type ChannelManagerOptions = {
   loadConfig: () => OpenClawConfig;
   channelLogs: Record<ChannelId, SubsystemLogger>;
   channelRuntimeEnvs: Record<ChannelId, RuntimeEnv>;
+  /**
+   * Gateway 启动时固定使用的插件注册表。
+   *
+   * 某些运行时路径会重新调用 loadOpenClawPlugins()，从而把全局 active
+   * registry 切到另一份对象。像 WeCom 这种在 channel 启动阶段动态注册
+   * HTTP 路由的插件，如果继续依赖"当前 active registry"，就可能把
+   * webhook 注册到错误的表里，最终 HTTP server 看不到路由。
+   */
+  pluginRegistry?: PluginRegistry;
   /**
    * Optional channel runtime helpers for external channel plugins.
    *
@@ -135,7 +146,7 @@ export type ChannelManager = {
 
 // Channel docking: lifecycle hooks (`plugin.gateway`) flow through this manager.
 export function createChannelManager(opts: ChannelManagerOptions): ChannelManager {
-  const { loadConfig, channelLogs, channelRuntimeEnvs, channelRuntime, resolveChannelRuntime } =
+  const { loadConfig, channelLogs, channelRuntimeEnvs, channelRuntime, resolveChannelRuntime, pluginRegistry } =
     opts;
 
   const channelStores = new Map<ChannelId, ChannelRuntimeStore>();
@@ -203,6 +214,15 @@ export function createChannelManager(opts: ChannelManagerOptions): ChannelManage
     return true;
   };
 
+  const ensureCanonicalPluginRegistry = () => {
+    if (!pluginRegistry) {
+      return;
+    }
+    if (getActivePluginRegistry() !== pluginRegistry) {
+      setActivePluginRegistry(pluginRegistry);
+    }
+  };
+
   const getStore = (channelId: ChannelId): ChannelRuntimeStore => {
     const existing = channelStores.get(channelId);
     if (existing) {
@@ -239,6 +259,7 @@ export function createChannelManager(opts: ChannelManagerOptions): ChannelManage
     accountId?: string,
     opts: StartChannelOptions = {},
   ) => {
+    ensureCanonicalPluginRegistry();
     const plugin = getChannelPlugin(channelId);
     const startAccount = plugin?.gateway?.startAccount;
     if (!startAccount) {
@@ -272,6 +293,7 @@ export function createChannelManager(opts: ChannelManagerOptions): ChannelManage
 
         // Reserve the account before the first await so overlapping start calls
         // cannot race into duplicate provider boots for the same account.
+        ensureCanonicalPluginRegistry();
         const abort = new AbortController();
         store.aborts.set(id, abort);
         let handedOffTask = false;
@@ -431,10 +453,12 @@ export function createChannelManager(opts: ChannelManagerOptions): ChannelManage
   };
 
   const startChannel = async (channelId: ChannelId, accountId?: string) => {
+    ensureCanonicalPluginRegistry();
     await startChannelInternal(channelId, accountId);
   };
 
   const stopChannel = async (channelId: ChannelId, accountId?: string) => {
+    ensureCanonicalPluginRegistry();
     const plugin = getChannelPlugin(channelId);
     const store = getStore(channelId);
     // Fast path: nothing running and no explicit plugin shutdown hook to run.
@@ -463,6 +487,7 @@ export function createChannelManager(opts: ChannelManagerOptions): ChannelManage
         manuallyStopped.add(restartKey(channelId, id));
         abort?.abort();
         if (plugin?.gateway?.stopAccount) {
+          ensureCanonicalPluginRegistry();
           const account = plugin.config.resolveAccount(cfg, id);
           await plugin.gateway.stopAccount({
             cfg,
@@ -493,6 +518,7 @@ export function createChannelManager(opts: ChannelManagerOptions): ChannelManage
   };
 
   const startChannels = async () => {
+    ensureCanonicalPluginRegistry();
     for (const plugin of listChannelPlugins()) {
       await startChannel(plugin.id);
     }
@@ -524,6 +550,7 @@ export function createChannelManager(opts: ChannelManagerOptions): ChannelManage
   };
 
   const getRuntimeSnapshot = (): ChannelRuntimeSnapshot => {
+    ensureCanonicalPluginRegistry();
     const cfg = loadConfig();
     const channels: ChannelRuntimeSnapshot["channels"] = {};
     const channelAccounts: ChannelRuntimeSnapshot["channelAccounts"] = {};
diff --git a/src/gateway/server.impl.ts b/src/gateway/server.impl.ts
index cec8f2cb4..a714b4dc9 100644
--- a/src/gateway/server.impl.ts
+++ b/src/gateway/server.impl.ts
@@ -628,6 +628,7 @@ export async function startGatewayServer(
     loadConfig,
     channelLogs,
     channelRuntimeEnvs,
+    pluginRegistry,
     resolveChannelRuntime: getChannelRuntime,
   });
   const getReadiness = createReadinessChecker({
